% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale-medium.R
\name{scale_medium_discrete}
\alias{scale_medium_discrete}
\title{Discrete scale for the drawing \code{medium} aesthetic}
\usage{
scale_medium_discrete(..., media = NULL)
}
\arguments{
\item{...}{Passed to \code{\link[ggplot2:discrete_scale]{ggplot2::discrete_scale()}} (e.g. \code{name}, \code{labels},
\code{guide}).}

\item{media}{Character vector of media to cycle through, each one of
\code{\link[=sketch_media]{sketch_media()}}. Defaults to every medium except \code{"pen"} (so mapped levels
look distinct); recycled with a warning if there are more levels than media.}
}
\value{
A \code{ggplot2} scale object.
}
\description{
Maps a discrete variable to drawing media (see \code{\link[=sketch_media]{sketch_media()}}) when it is
mapped with \code{aes(medium = )} on a path-like sketch geom
(\code{\link[=geom_sketch_line]{geom_sketch_line()}}, \code{\link[=geom_sketch_path]{geom_sketch_path()}}, \code{\link[=geom_sketch_segment]{geom_sketch_segment()}},
\code{\link[=geom_sketch_step]{geom_sketch_step()}}). Because of its name, ggplot2 picks it up automatically;
you only need to call it directly to choose which media to use or to set
legend options.
}
\details{
Each group is drawn in a single medium, so map \code{medium} to the same variable
you group by (often \code{colour} or \code{group}).
}
\examples{
library(ggplot2)
df <- data.frame(
  x = rep(1:10, 3),
  y = c(1:10, (1:10) + 4, (1:10) + 8),
  g = rep(c("a", "b", "c"), each = 10)
)
ggplot(df, aes(x, y, group = g, medium = g, colour = g)) +
  geom_sketch_line(linewidth = 1, seed = 1L) +
  scale_medium_discrete() +
  theme_sketch()
}
\seealso{
Other sketch-media:
\code{\link[=sketch_media]{sketch_media()}}
}
\concept{sketch-media}
