% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save-sketch.R
\name{ggsketch_save}
\alias{ggsketch_save}
\title{Save a sketch plot with a font-aware device}
\usage{
ggsketch_save(
  filename,
  plot = ggplot2::last_plot(),
  width = 8,
  height = 5,
  dpi = 300,
  device = NULL,
  ...
)
}
\arguments{
\item{filename}{File to write; the extension picks the device.}

\item{plot}{Plot to save. Default \code{\link[ggplot2:last_plot]{ggplot2::last_plot()}}.}

\item{width, height}{Size in inches. Defaults 8 x 5.}

\item{dpi}{Resolution for raster formats. Default 300.}

\item{device}{Override the chosen device (a function or name); passed
through to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} untouched when supplied.}

\item{...}{Other arguments passed on to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
Invisibly, \code{filename}.
}
\description{
A drop-in \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} wrapper that picks a device which can see
fonts registered with \code{\link[=register_sketch_font]{register_sketch_font()}} / \pkg{systemfonts}:
}
\details{
\itemize{
\item \code{.png} / \code{.jpeg} / \code{.jpg} / \code{.tiff} -- \pkg{ragg} when installed (falls
back to the ggsave default with a hint).
\item \code{.svg} -- \pkg{svglite} when installed.
\item \code{.pdf} -- \code{cairo_pdf} (embeds registered fonts; the base \code{pdf} device
does not).
\item \code{.eps} / \code{.ps} -- \code{cairo_ps}, with a warning: PostScript cannot embed
handwriting faces reliably, so prefer PDF.
}
}
\examples{
\donttest{
library(ggplot2)
p <- ggplot(mtcars, aes(wt, mpg)) + geom_sketch_point(seed = 1L) +
  theme_sketch()
out <- file.path(tempdir(), "sketch.png")
ggsketch_save(out, p)
unlink(out)
}
}
\seealso{
Other sketch-theme:
\code{\link{CoordSketch}},
\code{\link{CoordSketchPolar}},
\code{\link[=element_sketch_line]{element_sketch_line()}},
\code{\link[=ggsketch_check_fonts]{ggsketch_check_fonts()}},
\code{\link[=register_sketch_font]{register_sketch_font()}},
\code{\link[=scale_pressure_continuous]{scale_pressure_continuous()}},
\code{\link[=scale_roughness_continuous]{scale_roughness_continuous()}},
\code{\link{scale_sketch}},
\code{\link[=scale_tone_continuous]{scale_tone_continuous()}},
\code{\link[=sketch_palette]{sketch_palette()}},
\code{\link[=theme_sketch]{theme_sketch()}}
}
\concept{sketch-theme}
