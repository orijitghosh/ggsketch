% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coord-sketch.R
\name{CoordSketchPolar}
\alias{CoordSketchPolar}
\alias{coord_sketch_polar}
\title{A hand-drawn polar coordinate system}
\usage{
coord_sketch_polar(
  theta = "x",
  start = 0,
  direction = 1,
  clip = "on",
  roughness = 0.5,
  bowing = 0.5,
  n_passes = 2L,
  seed = NULL,
  rough_grid = TRUE
)
}
\arguments{
\item{theta}{Variable mapped to angle (\code{"x"} or \code{"y"}). Default \code{"x"}.}

\item{start}{Offset of the starting point, in radians. Default 0.}

\item{direction}{\code{1} clockwise, \code{-1} anticlockwise. Default 1.}

\item{clip}{Should drawing be clipped to the panel (\code{"on"}, default) or not
(\code{"off"})?}

\item{roughness, bowing, n_passes}{Sketch parameters for the grid. Gentle
defaults suited to gridlines (\code{0.5}, \code{0.5}, \code{2}).}

\item{seed}{Integer seed for reproducible wobble. \code{NULL} uses
\code{getOption("ggsketch.seed", 1L)}.}

\item{rough_grid}{Roughen the gridlines? Default \code{TRUE}.}
}
\value{
A \code{ggproto} Coord object to add to a plot.
}
\description{
The polar companion to \code{\link[=coord_sketch]{coord_sketch()}}: a drop-in replacement for
\code{\link[ggplot2:coord_polar]{ggplot2::coord_polar()}} that draws the circular grid hand-drawn. The radial
and angular gridlines are rendered as roughened sketch grobs, so pie/rose
charts and circular bar plots get a frame that matches the marks -- under any
theme. It reuses all of ggplot2's polar layout and only swaps how the grid is
drawn.
}
\examples{
library(ggplot2)
df <- data.frame(g = c("a", "b", "c", "d"), v = c(3, 5, 2, 4))
# A hand-drawn circular bar (rose) chart:
ggplot(df, aes(g, v, fill = g)) +
  geom_sketch_col(seed = 1L) +
  coord_sketch_polar(seed = 1L) +
  theme_sketch()
}
\seealso{
Other sketch-theme:
\code{\link{CoordSketch}},
\code{\link[=element_sketch_line]{element_sketch_line()}},
\code{\link[=ggsketch_check_fonts]{ggsketch_check_fonts()}},
\code{\link[=register_sketch_font]{register_sketch_font()}},
\code{\link[=scale_roughness_continuous]{scale_roughness_continuous()}},
\code{\link{scale_sketch}},
\code{\link[=scale_tone_continuous]{scale_tone_continuous()}},
\code{\link[=sketch_palette]{sketch_palette()}},
\code{\link[=theme_sketch]{theme_sketch()}}
}
\concept{sketch-theme}
