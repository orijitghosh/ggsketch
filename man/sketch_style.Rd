% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sketch-style.R
\name{sketch_style}
\alias{sketch_style}
\title{Apply a complete hand-drawn style in one call}
\usage{
sketch_style(style, palette = TRUE, ...)
}
\arguments{
\item{style}{One of \code{\link[=sketch_styles]{sketch_styles()}}.}

\item{palette}{If \code{TRUE} (default), include discrete colour and fill scales
using the style's palette. Set \code{FALSE} when a mapped colour/fill variable
is continuous (a discrete scale would error) or you want your own scale.}

\item{...}{Passed on to \code{\link[=theme_sketch]{theme_sketch()}} (e.g. \code{base_size}, \code{rough_frame},
\code{seed}). \code{paper} is fixed by the style and cannot be overridden here.}
}
\value{
A list of plot components (theme + optional scales) to add with \code{+}.
}
\description{
Bundles a \code{\link[=theme_sketch]{theme_sketch()}} paper ground, a qualitative colour + fill palette
tuned to that ground and (on ggplot2 >= 4.0) matching default geom ink into a
single object to add to a plot: \code{p + sketch_style("chalkboard")}. Styles:
}
\details{
\itemize{
\item \code{"notebook"} -- blue-ruled paper written in ballpoint/fountain-pen inks.
\item \code{"chalkboard"} -- dark board with chalky pastels; pair the line geoms with
\code{medium = "chalk"}.
\item \code{"blueprint"} -- cyanotype draughting: pale monoline strokes, warm accent.
\item \code{"field_notes"} -- kraft expedition journal in sepia and olive.
\item \code{"graphite"} -- plain ground, grey pencil tones; pair with
\code{medium = "pencil"}.
}
}
\examples{
library(ggplot2)
p <- ggplot(mpg, aes(displ, hwy, colour = drv)) +
  geom_sketch_point(seed = 1L)
p + sketch_style("field_notes")
p + sketch_style("chalkboard")
}
\seealso{
Other sketch-style:
\code{\link[=sketch_styles]{sketch_styles()}}
}
\concept{sketch-style}
