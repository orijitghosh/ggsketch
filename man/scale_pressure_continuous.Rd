% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale-pressure.R
\name{scale_pressure_continuous}
\alias{scale_pressure_continuous}
\alias{scale_pressure}
\title{Continuous scale for the sketch \code{pressure} aesthetic}
\usage{
scale_pressure_continuous(..., range = c(0.2, 1.6), guide = "none")

scale_pressure(..., range = c(0.2, 1.6), guide = "none")
}
\arguments{
\item{...}{Other arguments passed to \code{\link[ggplot2:continuous_scale]{ggplot2::continuous_scale()}}.}

\item{range}{Output width-multiplier range. Default \code{c(0.2, 1.6)}: the lightest
pressure draws the stroke at \verb{0.2x} its base width, the heaviest at \verb{1.6x}.}

\item{guide}{Legend guide. Defaults to \code{"none"} because the legend keys do not
reflect pressure; set to \code{"legend"} to show one anyway.}
}
\value{
A ggplot2 scale object.
}
\description{
\code{\link[=geom_sketch_line]{geom_sketch_line()}} and \code{\link[=geom_sketch_path]{geom_sketch_path()}} let you map a variable to
\code{pressure} (\code{aes(pressure = z)}) so the stroke swells and thins \strong{along} the
line, like a real pen pressed harder in places. This scale rescales that
variable's observed range to a band of width multipliers, just as
\code{scale_size()} rescales to a size range. It is applied automatically whenever
\code{pressure} is mapped to a continuous variable, so you only call it directly to
change \code{range}. To use values as raw width multipliers with no rescaling, wrap
them in \code{\link[base:I]{base::I()}} (\code{aes(pressure = I(z))}).
}
\details{
Mapping \code{pressure} renders the line through the variable-width
\code{\link[=stroke_ribbon]{stroke_ribbon()}} engine even under the default \code{medium = "pen"}, and combines
with a non-\code{pen} \code{medium} (the medium's own width profile is multiplied by the
mapped pressure).
}
\examples{
library(ggplot2)

# A line whose width tracks a variable, rescaled to c(0.2, 1.6) automatically.
ggplot(economics[1:120, ], aes(date, unemploy, pressure = unemploy)) +
  geom_sketch_line(linewidth = 1, seed = 1L)

# Widen the band for a more dramatic swell.
ggplot(economics[1:120, ], aes(date, unemploy, pressure = unemploy)) +
  geom_sketch_line(linewidth = 1, seed = 1L) +
  scale_pressure_continuous(range = c(0.05, 2.5))
}
\seealso{
Other sketch-theme:
\code{\link{CoordSketch}},
\code{\link{CoordSketchPolar}},
\code{\link[=element_sketch_line]{element_sketch_line()}},
\code{\link[=ggsketch_check_fonts]{ggsketch_check_fonts()}},
\code{\link[=ggsketch_save]{ggsketch_save()}},
\code{\link[=register_sketch_font]{register_sketch_font()}},
\code{\link[=scale_roughness_continuous]{scale_roughness_continuous()}},
\code{\link{scale_sketch}},
\code{\link[=scale_tone_continuous]{scale_tone_continuous()}},
\code{\link[=sketch_palette]{sketch_palette()}},
\code{\link[=theme_sketch]{theme_sketch()}}
}
\concept{sketch-theme}
