% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-engrave.R
\name{engrave_ladder}
\alias{engrave_ladder}
\title{Build a tonal hatch ladder for engraving fills}
\usage{
engrave_ladder(
  n_levels = 5L,
  base_gap = 0.1,
  gap_ratio = 0.62,
  base_angle = 45,
  cross_after = 3L,
  cross_angle = 90,
  tone_floor = 0.12,
  tone_ceiling = 0.92
)
}
\arguments{
\item{n_levels}{Number of hatch layers. Default 5.}

\item{base_gap}{Pitch (inches) of the sparsest layer. Each subsequent layer
tightens geometrically toward \code{base_gap * gap_ratio^(n_levels - 1)}.
Default 0.10.}

\item{gap_ratio}{Multiplicative pitch shrink per layer (0 < r <= 1; smaller =
faster densening). Default 0.62.}

\item{base_angle}{Angle (degrees) of the first layer. Default 45.}

\item{cross_after}{Layer index (1-based) at which cross-hatching begins; from
this layer on, angles alternate by \code{cross_angle}. Default 3.}

\item{cross_angle}{Angular offset (degrees) of the cross direction.
Default 90.}

\item{tone_floor, tone_ceiling}{Tone thresholds of the first and last layers;
the layers' thresholds are spread evenly between them. A region with tone
below \code{tone_floor} is left blank (paper); tone at or above \code{tone_ceiling}
gets every layer. Defaults 0.12 and 0.92.}
}
\value{
A list of layers, each \code{list(gap, angle, threshold)}.
}
\description{
An engraving ladder is an ordered list of hatch layers; each is applied only
where the tone field is at least its \code{threshold}, so darker regions
accumulate more (and finer, cross-hatched) layers. Defaults trace the classic
etching progression: a sparse base layer, then denser same-angle lines, then
a second angle (cross-hatch), then the fine angles that read as black.
}
\seealso{
Other sketch-core:
\code{\link[=curve_fill]{curve_fill()}},
\code{\link[=engrave_fill]{engrave_fill()}},
\code{\link[=hachure_fill]{hachure_fill()}},
\code{\link[=hachure_fill_multi]{hachure_fill_multi()}},
\code{\link[=rough_arc]{rough_arc()}},
\code{\link[=rough_bezier]{rough_bezier()}},
\code{\link[=rough_ellipse]{rough_ellipse()}},
\code{\link[=roughen_polyline]{roughen_polyline()}},
\code{\link[=sketch_fill]{sketch_fill()}},
\code{\link[=sketch_fill_multi]{sketch_fill_multi()}},
\code{\link[=spray_scatter]{spray_scatter()}},
\code{\link[=stroke_profile]{stroke_profile()}},
\code{\link[=stroke_ribbon]{stroke_ribbon()}},
\code{\link[=treemap_layout]{treemap_layout()}},
\code{\link[=watercolor_wash]{watercolor_wash()}},
\code{\link[=watercolor_wash_multi]{watercolor_wash_multi()}}
}
\concept{sketch-core}
