% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-sketch-text.R
\name{geom_sketch_text}
\alias{geom_sketch_text}
\alias{geom_sketch_label}
\title{Sketchy text and labels}
\usage{
geom_sketch_text(
  mapping = NULL,
  data = NULL,
  stat = "identity",
  position = "identity",
  ...,
  family = NULL,
  nudge_x = 0,
  nudge_y = 0,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)

geom_sketch_label(
  mapping = NULL,
  data = NULL,
  stat = "identity",
  position = "identity",
  ...,
  family = NULL,
  nudge_x = 0,
  nudge_y = 0,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}}.
Requires a \code{label} aesthetic.}

\item{data}{Data to display.}

\item{stat}{Statistical transformation (default \code{"identity"}).}

\item{position}{Position adjustment (default \code{"identity"}).}

\item{...}{Other arguments passed on to \code{\link[ggplot2:layer]{ggplot2::layer()}}, such as \code{size},
\code{colour}, \code{angle}, or \code{hjust}.}

\item{family}{Font family. By default the first installed handwriting face is
used; pass an explicit family to override, or \code{""} for the device default.}

\item{nudge_x, nudge_y}{Horizontal and vertical adjustment to nudge labels by.
Useful for offsetting text from points. Cannot be used together with an
explicit \code{position}.}

\item{na.rm}{If \code{FALSE} (default), missing values are removed with a warning.}

\item{show.legend}{Logical. Should this layer be included in the legend?}

\item{inherit.aes}{If \code{FALSE}, override the default aesthetics.}
}
\value{
A \code{ggplot2} layer object.
}
\description{
\code{geom_sketch_text()} and \code{geom_sketch_label()} add text in a handwriting font,
the sketch counterparts of \code{\link[ggplot2:geom_text]{ggplot2::geom_text()}} and \code{\link[ggplot2:geom_text]{ggplot2::geom_label()}}.
Unlike the other geoms the strokes are not geometrically roughened — the
hand-drawn feel comes from the font (see \code{\link[=ggsketch_check_fonts]{ggsketch_check_fonts()}} for which
faces are available). If no handwriting font is installed they render with the
device default family.
}
\examples{
library(ggplot2)
df <- data.frame(x = c(1, 2, 3), y = c(2, 3, 1),
                 lab = c("alpha", "bravo", "charlie"))

# `family = ""` uses the device default, so this runs on any device.
ggplot(df, aes(x, y, label = lab)) +
  geom_sketch_text(size = 6, family = "") +
  theme_sketch()

# With no `family`, the first installed handwriting font is used. Render with
# a font-capable device (ragg, svglite, cairo) to see it — the base pdf() /
# postscript() devices cannot use unregistered system fonts.
\dontrun{
ggplot(df, aes(x, y, label = lab)) +
  geom_sketch_text(size = 6) +
  theme_sketch()
}
}
\seealso{
Other sketch-geoms: 
\code{\link{GeomSketchAbline}},
\code{\link{GeomSketchBoxplot}},
\code{\link{GeomSketchCol}},
\code{\link{GeomSketchCurve}},
\code{\link{GeomSketchEllipse}},
\code{\link{GeomSketchHex}},
\code{\link{GeomSketchLine}},
\code{\link{GeomSketchLinerange}},
\code{\link{GeomSketchPath}},
\code{\link{GeomSketchPoint}},
\code{\link{GeomSketchPolygon}},
\code{\link{GeomSketchRect}},
\code{\link{GeomSketchRibbon}},
\code{\link{GeomSketchRug}},
\code{\link{GeomSketchSegment}},
\code{\link{GeomSketchSmooth}},
\code{\link{GeomSketchSpoke}},
\code{\link{GeomSketchViolin}},
\code{\link{annotate_sketch}()},
\code{\link{geom_sketch_bin2d}()},
\code{\link{geom_sketch_contour}()},
\code{\link{geom_sketch_count}()},
\code{\link{geom_sketch_density}()},
\code{\link{geom_sketch_density2d}()},
\code{\link{geom_sketch_function}()},
\code{\link{geom_sketch_histogram}()},
\code{\link{geom_sketch_jitter}()},
\code{\link{geom_sketch_qq}()},
\code{\link{geom_sketch_quantile}()}
}
\concept{sketch-geoms}
