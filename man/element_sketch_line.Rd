% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme-elements-sketch.R
\name{element_sketch_line}
\alias{element_sketch_line}
\alias{element_sketch_rect}
\title{Hand-drawn theme elements}
\usage{
element_sketch_line(
  colour = NULL,
  linewidth = NULL,
  linetype = NULL,
  lineend = NULL,
  color = NULL,
  roughness = 0.5,
  bowing = 0.5,
  n_passes = 2L,
  seed = NULL,
  ...
)

element_sketch_rect(
  fill = NULL,
  colour = NULL,
  linewidth = NULL,
  linetype = NULL,
  color = NULL,
  roughness = 0.6,
  bowing = 0.4,
  n_passes = 2L,
  seed = NULL,
  ...
)
}
\arguments{
\item{colour, color}{Line/border colour.}

\item{linewidth}{Line width.}

\item{linetype}{Line type.}

\item{lineend}{Line end style.}

\item{roughness, bowing, n_passes, seed}{Sketch parameters (see
\code{\link[=geom_sketch_path]{geom_sketch_path()}}). Defaults are gentle, suited to a frame.}

\item{...}{Passed to the underlying ggplot2 element constructor.}

\item{fill}{Fill colour (\code{element_sketch_rect()} only). \code{NA} draws the
outline only - the usual choice for a panel border.}
}
\value{
A ggplot2 theme element carrying an \verb{element_sketch_*} subclass.
}
\description{
Sketch counterparts of \code{\link[ggplot2:element]{ggplot2::element_line()}} and \code{\link[ggplot2:element]{ggplot2::element_rect()}}.
Use them in \code{\link[ggplot2:theme]{ggplot2::theme()}} (or via \code{theme_sketch(rough_frame = TRUE)}) to
render gridlines, panel borders, and axis ticks with the same wobbly,
double-stroke look as the geoms. They accept the usual element arguments plus
the shared sketch parameters (\code{roughness}, \code{bowing}, \code{n_passes}, \code{seed}).
}
\examples{
library(ggplot2)
ggplot(mtcars, aes(wt, mpg)) +
  geom_sketch_point(seed = 1L) +
  theme_sketch() +
  theme(panel.grid.major = element_sketch_line(colour = "grey80", seed = 7L))
}
\seealso{
Other sketch-theme: 
\code{\link{CoordSketch}},
\code{\link{ggsketch_check_fonts}()},
\code{\link{register_sketch_font}()},
\code{\link{scale_roughness_continuous}()},
\code{\link{scale_sketch}},
\code{\link{scale_tone_continuous}()},
\code{\link{sketch_palette}()},
\code{\link{theme_sketch}()}
}
\concept{sketch-theme}
