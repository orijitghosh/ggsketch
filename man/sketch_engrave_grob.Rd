% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grob-sketch.R
\name{sketch_engrave_grob}
\alias{sketch_engrave_grob}
\title{Create a sketchy engraving grob (tonal cross-hatch by line density)}
\usage{
sketch_engrave_grob(
  rings,
  field,
  ladder = NULL,
  ladder_levels = 5L,
  ladder_base_gap = 0.08,
  ladder_gap_ratio = 0.62,
  ladder_base_angle = 45,
  ladder_cross_after = 3L,
  roughness = 0.5,
  bowing = 0.3,
  seed = NULL,
  min_gap_in = 0.012,
  gp = gpar(),
  name = NULL,
  vp = NULL
)
}
\arguments{
\item{rings}{A list of rings, each a list with npc [0,1] \code{x} and \code{y} vertex
vectors bounding the region to engrave (even-odd holes honoured).}

\item{field}{A vectorised tone function \verb{function(x, y)} taking npc [0,1]
coordinates and returning tone in \verb{[0, 1]} (0 = paper, 1 = darkest).}

\item{ladder}{A ladder from \code{\link[=engrave_ladder]{engrave_ladder()}}; \code{NULL} builds a default from
the \verb{ladder_*} parameters.}

\item{ladder_levels, ladder_base_gap, ladder_gap_ratio, ladder_base_angle, ladder_cross_after}{Ladder
controls used when \code{ladder} is \code{NULL}. \code{ladder_base_gap} is an npc-x
fraction (converted to inches at draw time).}

\item{roughness, bowing, seed}{Sketch parameters for the engraving strokes.}

\item{min_gap_in}{Hard pitch floor in inches: ladder layers finer than this
are dropped, so the darkest tones cannot explode into a runaway number of
strokes. Default 0.012.}

\item{gp}{\code{gpar()} for the engraving strokes (\code{col}, \code{lwd}).}

\item{name, vp}{Passed to \code{grid::gTree()}.}
}
\value{
A \code{SketchEngraveGrob} grob subclass.
}
\description{
Fills a region with \code{\link[=engrave_fill]{engrave_fill()}}: a ladder of hatch layers whose
accumulated density follows a tone field, so the region shades continuously
from blank paper (light) to dense cross-hatch (dark) the way an etching does.
The hatch geometry is computed in device inches (so angles and wobble are
device-consistent); the \code{field} is supplied in npc [0,1] and sampled through
an inch-to-npc affine at draw time.
}
\seealso{
Other grob-layer:
\code{\link[=sketch_arrow_grob]{sketch_arrow_grob()}},
\code{\link[=sketch_band_grob]{sketch_band_grob()}},
\code{\link[=sketch_callout_grob]{sketch_callout_grob()}},
\code{\link[=sketch_dotplot_grob]{sketch_dotplot_grob()}},
\code{\link[=sketch_ellipse_grob]{sketch_ellipse_grob()}},
\code{\link[=sketch_path_grob]{sketch_path_grob()}},
\code{\link[=sketch_polygon_grob]{sketch_polygon_grob()}},
\code{\link[=sketch_spray_grob]{sketch_spray_grob()}},
\code{\link[=sketch_stroke_grob]{sketch_stroke_grob()}},
\code{\link[=sketch_wedge_grob]{sketch_wedge_grob()}}
}
\concept{grob-layer}
