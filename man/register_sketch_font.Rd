% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fonts-sketch.R
\name{register_sketch_font}
\alias{register_sketch_font}
\title{Register a handwriting font for reproducible sketch text}
\usage{
register_sketch_font(
  family,
  plain,
  bold = plain,
  italic = plain,
  bolditalic = plain,
  ...
)
}
\arguments{
\item{family}{Family name to register the font under (e.g. \code{"Caveat"}). This
is the name you then pass to \verb{family =} or \verb{base_family =}.}

\item{plain}{Path to the regular/plain font file (\code{.ttf} or \code{.otf}).}

\item{bold, italic, bolditalic}{Optional paths to the bold/italic faces;
default to \code{plain}.}

\item{...}{Passed to \code{\link[systemfonts:register_font]{systemfonts::register_font()}}.}
}
\value{
Invisibly, the registered \code{family} name.
}
\description{
Registers a font file under a family name with \pkg{systemfonts} so that
\code{\link[=geom_sketch_text]{geom_sketch_text()}}, \code{\link[=theme_sketch]{theme_sketch()}} (\code{base_family = "auto"}), and the
font resolver can find it on font-aware devices (ragg, svglite, cairo)
without installing the font system-wide. Call it once per session (e.g. in a
script or \code{.Rprofile}); ship the \code{.ttf}/\code{.otf} alongside your project for
fully reproducible output.
}
\examples{
\dontshow{if (requireNamespace("systemfonts", quietly = TRUE)) withAutoprint(\{ # examplesIf}
# Register any font file under a family name you choose. Here we reuse a font
# already on the system; in practice point `plain` at a handwriting .ttf/.otf
# (e.g. Caveat from Google Fonts).
f <- systemfonts::system_fonts()$path[1]
register_sketch_font("MySketchFont", f)
\dontshow{\}) # examplesIf}
}
\seealso{
Other sketch-theme:
\code{\link{CoordSketch}},
\code{\link{CoordSketchPolar}},
\code{\link[=element_sketch_line]{element_sketch_line()}},
\code{\link[=ggsketch_check_fonts]{ggsketch_check_fonts()}},
\code{\link[=scale_roughness_continuous]{scale_roughness_continuous()}},
\code{\link{scale_sketch}},
\code{\link[=scale_tone_continuous]{scale_tone_continuous()}},
\code{\link[=sketch_palette]{sketch_palette()}},
\code{\link[=theme_sketch]{theme_sketch()}}
}
\concept{sketch-theme}
