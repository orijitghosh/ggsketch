% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale-tone.R
\name{scale_tone_continuous}
\alias{scale_tone_continuous}
\alias{scale_engrave}
\title{Continuous scale for the engraving \code{tone} aesthetic}
\usage{
scale_tone_continuous(..., range = c(0.1, 0.95), guide = "none")

scale_engrave(..., range = c(0.1, 0.95), guide = "none")
}
\arguments{
\item{...}{Other arguments passed to \code{\link[ggplot2:continuous_scale]{ggplot2::continuous_scale()}}.}

\item{range}{Output tone range, within \verb{[0, 1]}. Default \code{c(0.1, 0.95)}:
\code{0.1} is the faintest hatch that still reads and \code{0.95} is near-solid
black. Give a decreasing range (e.g. \code{c(0.95, 0.1)}) to invert the mapping.}

\item{guide}{Legend guide. Defaults to \code{"none"} because the legend keys do
not reflect tone; set to \code{"legend"} to show one anyway.}
}
\value{
A ggplot2 scale object.
}
\description{
\code{\link[=geom_sketch_shade]{geom_sketch_shade()}} shades each region by a \code{tone} aesthetic in \verb{[0, 1]}
(0 = blank paper, 1 = densest cross-hatch). Mapping a raw variable to \code{tone}
(\code{aes(tone = z)}) rescales its observed range to a legible tone band with this
scale, just as \code{scale_size()} rescales to a size range. It is applied
automatically whenever \code{tone} is mapped to a continuous variable, so you only
call it directly to change \code{range} (or to reverse it by giving a decreasing
\code{range}). To use values as raw tone with no rescaling, wrap them in
\code{\link[base:AsIs]{base::I()}} (\code{aes(tone = I(z))}).
}
\examples{
library(ggplot2)

hex <- data.frame(
  x = cos(seq(0, 2 * pi, length.out = 7))[-7],
  y = sin(seq(0, 2 * pi, length.out = 7))[-7]
)
regions <- do.call(rbind, lapply(1:3, function(k) {
  transform(hex, x = x + (k - 1) * 2.3, g = k, val = k)
}))

# `val` is rescaled to the default tone band c(0.1, 0.95) automatically.
ggplot(regions, aes(x, y, group = g)) +
  geom_sketch_shade(aes(tone = val), seed = 1L) +
  coord_equal()

# Push the darkest region all the way to solid black.
ggplot(regions, aes(x, y, group = g)) +
  geom_sketch_shade(aes(tone = val), seed = 1L) +
  scale_tone_continuous(range = c(0.15, 1)) +
  coord_equal()
}
\seealso{
Other sketch-theme: 
\code{\link{CoordSketch}},
\code{\link{CoordSketchPolar}},
\code{\link{element_sketch_line}()},
\code{\link{ggsketch_check_fonts}()},
\code{\link{register_sketch_font}()},
\code{\link{scale_roughness_continuous}()},
\code{\link{scale_sketch}},
\code{\link{sketch_palette}()},
\code{\link{theme_sketch}()}
}
\concept{sketch-theme}
