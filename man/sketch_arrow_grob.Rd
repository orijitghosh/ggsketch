% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grob-sketch.R
\name{sketch_arrow_grob}
\alias{sketch_arrow_grob}
\title{Create a sketchy arrow grob}
\usage{
sketch_arrow_grob(
  x0,
  y0,
  cx,
  cy,
  x1,
  y1,
  roughness = 1,
  bowing = 1,
  n_passes = 2L,
  seed = NULL,
  arrow_length = NULL,
  arrow_angle = 25,
  arrow_type = "open",
  gp = gpar(),
  name = NULL,
  vp = NULL
)
}
\arguments{
\item{x0, y0}{Shaft start in npc [0,1] (vectors, one per arrow).}

\item{cx, cy}{Quadratic-Bezier control point in npc (vectors). For a straight
arrow pass the chord midpoint.}

\item{x1, y1}{Shaft end / arrow tip in npc (vectors).}

\item{roughness, bowing, n_passes, seed}{Sketch parameters.}

\item{arrow_length}{Arrowhead length in inches. \code{NULL} (default) adapts it to
the shaft length.}

\item{arrow_angle}{Half-angle of the arrowhead in degrees. Default 25.}

\item{arrow_type}{\code{"open"} (default) draws a two-stroke V; \code{"closed"} draws a
filled rough triangle.}

\item{gp}{A \code{grid::gpar()} for the strokes (per-arrow \code{col} recycled).}

\item{name, vp}{Passed to \code{grid::gTree()}.}
}
\value{
A \code{SketchArrowGrob} grob subclass.
}
\description{
Draws one or more hand-drawn arrows. The shaft is a quadratic Bezier (so it
can curve) and the arrowhead is roughened and oriented to the curve's end
tangent, both assembled in device inches so the head stays crisp and
correctly angled on any panel shape. The arrowhead size can adapt to the
shaft length.
}
\seealso{
Other grob-layer: 
\code{\link{sketch_band_grob}()},
\code{\link{sketch_callout_grob}()},
\code{\link{sketch_dotplot_grob}()},
\code{\link{sketch_ellipse_grob}()},
\code{\link{sketch_path_grob}()},
\code{\link{sketch_polygon_grob}()},
\code{\link{sketch_wedge_grob}()}
}
\concept{grob-layer}
