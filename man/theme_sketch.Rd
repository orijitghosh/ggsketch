% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme-sketch.R
\name{theme_sketch}
\alias{theme_sketch}
\title{A hand-drawn theme for ggplot2}
\usage{
theme_sketch(
  base_size = 11,
  base_family = "",
  base_line_size = base_size/22,
  base_rect_size = base_size/22,
  dark = FALSE
)
}
\arguments{
\item{base_size}{Base font size (default 11).}

\item{base_family}{Base font family. \code{""} (default) uses the device default.
\code{"auto"} picks the first installed handwriting font (see
\code{\link[=ggsketch_check_fonts]{ggsketch_check_fonts()}}), falling back to the device default. Or pass an
explicit family name.}

\item{base_line_size}{Line size (default \code{base_size / 22}).}

\item{base_rect_size}{Rect size (default \code{base_size / 22}).}

\item{dark}{If \code{TRUE}, use the dark "chalkboard" preset. Default \code{FALSE}
(light "paper" preset).}
}
\value{
A \code{ggplot2::theme} object.
}
\description{
A sketch-style theme based on \code{\link[ggplot2:ggtheme]{ggplot2::theme_bw()}} with a paper-and-ink
palette that complements the rough geoms.  Light (default) and dark presets
are available via \code{dark}.  The sketchiness of the \emph{marks} comes from the
geoms themselves; this theme styles the surrounding frame, typography, and
background.
}
\examples{
library(ggplot2)
p <- ggplot(mtcars, aes(wt, mpg)) + geom_sketch_point(seed = 1L)
p + theme_sketch()
p + theme_sketch(dark = TRUE)
}
\seealso{
Other sketch-theme: 
\code{\link{ggsketch_check_fonts}()}
}
\concept{sketch-theme}
