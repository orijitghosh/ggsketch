% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/animate-gganimate.R
\name{boil_gganimate}
\alias{boil_gganimate}
\title{Boil a gganimate animation (sketch wobble + data transitions)}
\usage{
boil_gganimate(
  animation,
  nframes = 100L,
  fps = 10,
  intensity = 1,
  detail = 1L,
  file = NULL,
  width = 7,
  height = 5,
  units = "in",
  res = 120,
  background = "white",
  device = NULL,
  seed = NULL,
  renderer = c("auto", "gifski", "magick", "none"),
  loop = TRUE
)
}
\arguments{
\item{animation}{A \pkg{gganimate} animation: a ggplot using ggsketch geoms
with a \verb{transition_*()} added (class \code{gganim}).}

\item{nframes}{Number of frames. Default 100 (gganimate's default).}

\item{fps}{Frames per second in the output. Default 10.}

\item{intensity}{Boil strength: scales how far the seed jitter steps each
frame. 1 (default) matches \code{\link[=animate_sketch]{animate_sketch()}}; higher shimmers more.}

\item{detail}{Tween sub-frames per frame, passed through to gganimate's
prerender for smoother motion (rendered frames are sampled back down to
\code{nframes}). Default 1.}

\item{file}{Output path (e.g. a \code{.gif}). If \code{NULL} (default), no file is
written and the frame paths are returned invisibly.}

\item{width, height, units, res}{Frame size and resolution. \code{units} one of
\code{"in"}, \code{"cm"}, \code{"mm"}, \code{"px"}.}

\item{background}{Frame background colour. Default \code{"white"}.}

\item{device}{Graphics device: \code{NULL} (default; \code{"ragg"} when installed, else
\code{"png"}), \code{"ragg"}/\code{"png"}, or any device string gganimate accepts
(\code{"ragg_png"}, \code{"svglite"}, ...).}

\item{seed}{Integer offset for the boil sequence (\code{NULL} = 0, so frame 1 is
un-boiled). Change it to vary the shimmer without touching the plot.}

\item{renderer}{GIF backend: \code{"auto"} (default), \code{"gifski"}, \code{"magick"}, or
\code{"none"} (always return frame paths).}

\item{loop}{Loop the output forever? Default \code{TRUE}.}
}
\value{
Invisibly, the output path (when written) or a character vector of
frame image paths.
}
\description{
Render a \pkg{gganimate} animation - any plot built with ggsketch geoms plus a
\verb{transition_*()} - so that, on top of gganimate's data tweening, the
hand-drawn lines \emph{boil}: every roughening seed is shifted once per frame, so
the drawing shimmers and re-draws itself like a hand-animated cel while the
bars grow, points fly, or a line draws itself along \code{x}. It is the moving-data
companion to \code{\link[=animate_sketch]{animate_sketch()}}, which boils a static plot.
}
\details{
The boil rides on the global \code{ggsketch.seed_jitter} option (the same lever
\code{\link[=animate_sketch]{animate_sketch()}} uses), advanced by a per-frame counter so frame 1 is the
un-boiled drawing and the whole animation is reproducible. Frames are stitched
with the same backend as \code{\link[=animate_sketch]{animate_sketch()}} (\pkg{gifski} or \pkg{magick});
if neither is installed the frame paths are returned. \pkg{gganimate} itself
is an optional dependency.
}
\examples{
\dontshow{if (requireNamespace("gganimate", quietly = TRUE) && requireNamespace("gifski", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\donttest{
library(ggplot2)
library(gganimate)
p <- ggplot(mpg, aes(class, fill = drv)) +
  geom_sketch_bar(position = "dodge", seed = 1L) +
  scale_fill_sketch() +
  theme_sketch() +
  transition_states(drv, transition_length = 2, state_length = 1)
boil_gganimate(p, nframes = 30, file = tempfile(fileext = ".gif"))
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=animate_sketch]{animate_sketch()}} for boiling or drawing-on a static plot.
}
