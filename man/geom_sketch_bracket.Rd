% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-sketch-bracket.R
\docType{data}
\name{GeomSketchBracket}
\alias{GeomSketchBracket}
\alias{geom_sketch_bracket}
\title{Sketchy significance / comparison brackets}
\format{
An object of class \code{GeomSketchBracket} (inherits from \code{Geom}, \code{ggproto}, \code{gg}) of length 6.
}
\usage{
GeomSketchBracket

geom_sketch_bracket(
  mapping = NULL,
  data = NULL,
  stat = "identity",
  position = "identity",
  ...,
  tip_length = 0.02,
  family = NULL,
  label_vjust = -0.35,
  roughness = 0.8,
  bowing = 0.4,
  n_passes = 2L,
  seed = NULL,
  na.rm = FALSE,
  show.legend = FALSE,
  inherit.aes = FALSE
)
}
\arguments{
\item{mapping}{Aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}}. Requires
\code{xmin}, \code{xmax}, and \code{y}; \code{label} is optional.}

\item{data}{Data with one row per bracket.}

\item{stat}{Statistical transformation. Default \code{"identity"}.}

\item{position}{Position adjustment. Default \code{"identity"}.}

\item{...}{Other arguments passed on to the layer.}

\item{tip_length}{Length of the downward end tips, as a fraction of panel
height. Default \code{0.02}. Use \code{0} for a plain bar.}

\item{family}{Font family for the label. Defaults to the same family as
\code{\link[=theme_sketch]{theme_sketch()}} (\code{getOption("ggsketch.base_family", "")}, i.e. the device
default), so the label matches the plot's other text; set
\code{options(ggsketch.base_family = "auto")} for a handwriting face, or pass an
explicit family here.}

\item{label_vjust}{Vertical justification of the label relative to the bar
(negative nudges it above). Default \code{-0.35}.}

\item{roughness}{Non-negative roughness (0 = straight). Default 0.8.}

\item{bowing}{Non-negative bowing multiplier. Default 0.4 (kept low so the bar
stays readable).}

\item{n_passes}{Number of stroke passes. Default 2.}

\item{seed}{Integer seed. \code{NULL} uses \code{getOption("ggsketch.seed", 1L)}.}

\item{na.rm}{Remove missing values silently? Default \code{FALSE}.}

\item{show.legend}{Logical; include in legend? Default \code{FALSE}.}

\item{inherit.aes}{Inherit aesthetics from the plot? Default \code{FALSE}.}
}
\value{
A \code{ggplot2} layer object.
}
\description{
Draws a hand-drawn bracket spanning \code{xmin} to \code{xmax} at height \code{y}, with short
tips dropping toward the data and an optional \code{label} (e.g. a p-value or
"n.s.") centred above. It is the sketch counterpart of a \code{ggsignif} bracket:
useful for marking pairwise comparisons on boxplots, bars, or violins.
}
\details{
Brackets are usually one-off annotations, so supply them with their own
\code{data} and \code{inherit.aes = FALSE} rather than inheriting the plot's mapping.
}
\examples{
library(ggplot2)
brackets <- data.frame(xmin = 1, xmax = 2, y = 45, label = "p = 0.01")
ggplot(mpg, aes(drv, hwy)) +
  geom_sketch_boxplot(seed = 1L) +
  geom_sketch_bracket(
    data = brackets,
    aes(xmin = xmin, xmax = xmax, y = y, label = label),
    family = "", seed = 2L
  ) +
  theme_sketch()
}
\seealso{
Other sketch-geoms: 
\code{\link{GeomSketchAbline}},
\code{\link{GeomSketchArrow}},
\code{\link{GeomSketchBoxplot}},
\code{\link{GeomSketchCallout}},
\code{\link{GeomSketchCol}},
\code{\link{GeomSketchCurve}},
\code{\link{GeomSketchEllipse}},
\code{\link{GeomSketchHex}},
\code{\link{GeomSketchLine}},
\code{\link{GeomSketchLinerange}},
\code{\link{GeomSketchMarkHull}},
\code{\link{GeomSketchPath}},
\code{\link{GeomSketchPoint}},
\code{\link{GeomSketchPolygon}},
\code{\link{GeomSketchRect}},
\code{\link{GeomSketchRibbon}},
\code{\link{GeomSketchRug}},
\code{\link{GeomSketchSegment}},
\code{\link{GeomSketchSmooth}},
\code{\link{GeomSketchSpoke}},
\code{\link{GeomSketchViolin}},
\code{\link{StatSketchPie}},
\code{\link{annotate_sketch}()},
\code{\link{annotate_sketch_arrow}()},
\code{\link{annotate_sketch_callout}()},
\code{\link{geom_sketch_bin2d}()},
\code{\link{geom_sketch_contour}()},
\code{\link{geom_sketch_count}()},
\code{\link{geom_sketch_density}()},
\code{\link{geom_sketch_density2d}()},
\code{\link{geom_sketch_function}()},
\code{\link{geom_sketch_histogram}()},
\code{\link{geom_sketch_jitter}()},
\code{\link{geom_sketch_qq}()},
\code{\link{geom_sketch_quantile}()},
\code{\link{geom_sketch_text}()}
}
\concept{sketch-geoms}
\keyword{datasets}
