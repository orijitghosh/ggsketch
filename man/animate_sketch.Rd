% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/animate.R
\name{animate_sketch}
\alias{animate_sketch}
\title{Animate a sketch plot by "boiling" its lines}
\usage{
animate_sketch(
  plot,
  nframes = 12L,
  fps = 10,
  type = c("boil", "draw_on"),
  direction = c("lr", "rl", "bt", "tb", "diag", "radial"),
  easing = c("linear", "ease_in", "ease_out", "ease_in_out"),
  file = NULL,
  width = 7,
  height = 5,
  units = "in",
  res = 120,
  background = "white",
  device = NULL,
  seed = NULL,
  renderer = c("auto", "gifski", "magick", "none"),
  loop = TRUE
)
}
\arguments{
\item{plot}{A ggplot object using ggsketch geoms.}

\item{nframes}{Number of frames. Default 12.}

\item{fps}{Frames per second in the output GIF. Default 10.}

\item{type}{Motion type: \code{"boil"} (re-seed the wobble each frame so the whole
drawing shimmers) or \code{"draw_on"} (progressively reveal the finished drawing
behind a moving wipe, as if a hand were drawing it).}

\item{direction}{For \code{type = "draw_on"}, how the drawing is revealed: a
straight wipe \code{"lr"} (left-to-right, default), \code{"rl"}, \code{"bt"}
(bottom-to-top) or \code{"tb"}; a diagonal wipe \code{"diag"} (top-left to
bottom-right); or \code{"radial"} (a circular iris opening from the centre).}

\item{easing}{For \code{type = "draw_on"}, the pacing of the reveal across frames:
\code{"linear"} (constant speed, default), \code{"ease_in"} (start slow), \code{"ease_out"}
(end slow), or \code{"ease_in_out"} (slow at both ends).}

\item{file}{Output GIF path. If \code{NULL} (default), no GIF is written and the
frame paths are returned invisibly.}

\item{width, height, units, res}{Frame size and resolution, passed to the
graphics device (\code{units} one of \code{"in"}, \code{"cm"}, \code{"mm"}, \code{"px"}).}

\item{background}{Frame background colour. Default \code{"white"}.}

\item{device}{Graphics device: \code{"ragg"} (default when installed) or \code{"png"}.}

\item{seed}{Base seed (\code{NULL} uses \code{getOption("ggsketch.seed", 1L)}).}

\item{renderer}{GIF backend: \code{"auto"} (default), \code{"gifski"}, \code{"magick"}, or
\code{"none"} (always return frame paths).}

\item{loop}{Loop the GIF forever? Default \code{TRUE}.}
}
\value{
Invisibly, the GIF path (when written) or a character vector of frame
image paths.
}
\description{
Renders a ggplot built with ggsketch geoms \code{nframes} times, shifting every
roughening seed by a per-frame amount, so the whole drawing shimmers and
re-draws itself like a hand-animated cel (the "boiling line" effect). Because
the shift is added to \emph{every} resolved seed -- whether a layer set its own
\code{seed} or inherited the global one -- a plot boils without any change to its
code. Frame 1 reproduces the static plot exactly.
}
\details{
Frames are stitched into a GIF when \pkg{gifski} or \pkg{magick} is installed
(neither is a hard dependency); otherwise the frame image paths are returned
so you can assemble them yourself. Everything is reproducible: the same \code{seed}
yields the same animation.
}
\examples{
\donttest{
library(ggplot2)
p <- ggplot(mpg, aes(class)) +
  geom_sketch_bar(fill = "#7BAFD4", seed = 1L) +
  theme_sketch()
# Write a GIF if gifski/magick is available, else get frame paths back:
gif <- animate_sketch(p, nframes = 8, file = tempfile(fileext = ".gif"))
}
}
