% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-paper.R
\name{paper_primitives}
\alias{paper_primitives}
\title{Build the vector primitives for a paper ground}
\usage{
paper_primitives(kind, width_in = 6, height_in = 4, seed = NULL)
}
\arguments{
\item{kind}{A value from \code{\link[=sketch_papers]{sketch_papers()}}.}

\item{width_in, height_in}{Panel size in inches (sets the ruling pitch).}

\item{seed}{Integer seed (for aged blotches).}
}
\value{
A list with \code{ground} (fill colour) and zero or more of \code{segs} (a list
of homogeneous line groups, each \code{list(x0, y0, x1, y1, colour, lwd)}),
\code{dots} (\code{list(x, y, r_in, colour)}), and \code{blotches} (a list of
\code{list(x, y, fill)} polygons), or \code{NULL} for \code{"none"}.
}
\description{
Turns a \code{\link[=paper_spec]{paper_spec()}} into draw-ready primitives in npc coordinates, spaced
for the given physical panel size. The grob layer (\code{element_sketch_paper()})
renders the result; this function stays free of any \code{grid} dependency.
}
\seealso{
Other sketch-paper: 
\code{\link{element_sketch_paper}()},
\code{\link{paper_spec}()},
\code{\link{sketch_papers}()}
}
\concept{sketch-paper}
