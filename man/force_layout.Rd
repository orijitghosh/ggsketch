% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-network.R
\name{force_layout}
\alias{force_layout}
\title{Force-directed graph layout (Fruchterman-Reingold)}
\usage{
force_layout(edges, n_nodes = NULL, niter = 500L, seed = NULL)
}
\arguments{
\item{edges}{A two-column matrix or data frame of \strong{1-based integer node
indices}, one row per edge (\code{from}, \code{to}). May have zero rows (an
edgeless graph, laid out on a circle).}

\item{n_nodes}{Number of nodes. Defaults to the largest index in \code{edges}
(so isolated high-index nodes need this set explicitly).}

\item{niter}{Number of iterations. Default 500.}

\item{seed}{Integer seed for the initial placement. \code{NULL} uses
\code{getOption("ggsketch.seed", 1L)}. The layout is otherwise deterministic.}
}
\value{
A data frame with columns \code{x} and \code{y}, one row per node in index
order.
}
\description{
A pure-R implementation of the Fruchterman-Reingold force-directed layout,
so \code{\link[=geom_sketch_node]{geom_sketch_node()}} / \code{\link[=geom_sketch_edge]{geom_sketch_edge()}} can place a network with no
external graph dependency. Repulsive forces push every node apart; attractive
forces pull edge-connected nodes together; a cooling schedule settles the
system. Coordinates are returned rescaled to roughly \verb{[-1, 1]} on both axes.
}
\examples{
# A small ring of five nodes
e <- cbind(1:5, c(2:5, 1))
force_layout(e, seed = 1L)
}
