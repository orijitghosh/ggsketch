% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-stroke.R
\name{stroke_ribbon}
\alias{stroke_ribbon}
\title{Build a variable-width stroke as a closed polygon ribbon}
\usage{
stroke_ribbon(
  x,
  y,
  width,
  taper = c("none", "both", "start", "end"),
  taper_frac = 0,
  pressure = NULL,
  nib_angle = NULL,
  nib_floor = 0.15,
  jitter_w = 0,
  cap = c("round", "butt"),
  miter_limit = 3,
  seed = NULL
)
}
\arguments{
\item{x, y}{Numeric vectors of centreline vertices in inch space (same length).}

\item{width}{Full stroke width in inches: a scalar, or a per-vertex vector.}

\item{taper}{Where the stroke narrows to a tip: \code{"none"} (default), \code{"both"},
\code{"start"}, or \code{"end"}.}

\item{taper_frac}{Tip width as a fraction of \code{width} (\code{0} = sharp point,
\code{1} = no narrowing). Default \code{0}.}

\item{pressure}{Optional vectorised function \code{f(t)} over normalised
arc-length \code{t} in \verb{[0, 1]} returning a width multiplier (see
\code{\link[=stroke_profile]{stroke_profile()}}). \code{NULL} (default) is constant pressure.}

\item{nib_angle}{Optional broad-nib angle in degrees for a calligraphic
stroke: the half-width is scaled by \verb{|sin(segment_dir - nib_angle)|} (with a
small floor), so the line is thick across the nib and thin along it. \code{NULL}
(default) disables it.}

\item{nib_floor}{Minimum nib multiplier in \verb{[0, 1]}, so a calligraphic stroke
never fully vanishes. Default \code{0.15}.}

\item{jitter_w}{Width roughening in \verb{[0, 1]}: random per-vertex modulation of
the half-width, for a dry / inky edge. Default \code{0}.}

\item{cap}{End-cap style: \code{"round"} (default) or \code{"butt"}.}

\item{miter_limit}{Clamp on the joint miter factor, capping how far an outside
corner extends at a sharp turn. Default \code{3}.}

\item{seed}{Integer seed for \code{jitter_w} (ADR-0004).}
}
\value{
A 2-column \verb{(x, y)} matrix giving the closed ribbon polygon (right side
forward, end cap, left side back, start cap). Fill it with the stroke colour
and no border for a solid variable-width stroke.
}
\description{
Offsets a centreline left and right by a per-vertex half-width and closes the
loop into a single polygon, so a hand-drawn stroke can taper, swell with
pressure, or vary like a broad calligraphic nib -- effects \code{grid} cannot do
with a constant-\code{lwd} polyline. The centreline is used as supplied (roughen it
first with \code{\link[=roughen_polyline]{roughen_polyline()}} for a sketchy edge); this routine only offsets,
so it is deterministic apart from \code{jitter_w}.
}
\seealso{
Other sketch-core:
\code{\link[=arrowhead]{arrowhead()}},
\code{\link[=curve_fill]{curve_fill()}},
\code{\link[=engrave_fill]{engrave_fill()}},
\code{\link[=engrave_ladder]{engrave_ladder()}},
\code{\link[=hachure_fill]{hachure_fill()}},
\code{\link[=hachure_fill_multi]{hachure_fill_multi()}},
\code{\link[=leader_path]{leader_path()}},
\code{\link[=repel_layout]{repel_layout()}},
\code{\link[=rough_arc]{rough_arc()}},
\code{\link[=rough_bezier]{rough_bezier()}},
\code{\link[=rough_ellipse]{rough_ellipse()}},
\code{\link[=roughen_polyline]{roughen_polyline()}},
\code{\link[=sketch_arrowheads]{sketch_arrowheads()}},
\code{\link[=sketch_fill]{sketch_fill()}},
\code{\link[=sketch_fill_multi]{sketch_fill_multi()}},
\code{\link[=spray_scatter]{spray_scatter()}},
\code{\link[=stroke_profile]{stroke_profile()}},
\code{\link[=treemap_layout]{treemap_layout()}},
\code{\link[=wash_bleed]{wash_bleed()}},
\code{\link[=watercolor_wash]{watercolor_wash()}},
\code{\link[=watercolor_wash_multi]{watercolor_wash_multi()}}
}
\concept{sketch-core}
