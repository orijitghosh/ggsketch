% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grob-sketch.R
\name{sketch_wedge_grob}
\alias{sketch_wedge_grob}
\title{Create a sketchy pie/donut-wedge grob}
\usage{
sketch_wedge_grob(
  x0,
  y0,
  r,
  r0 = 0,
  start,
  end,
  roughness = 1,
  bowing = 0.4,
  n_passes = 2L,
  seed = NULL,
  fill_style = "solid",
  hachure_angle = 45,
  hachure_gap = 0.07,
  fill_weight = 0.5,
  fill_gp = gpar(),
  outline_gp = gpar(),
  name = NULL,
  vp = NULL
)
}
\arguments{
\item{x0, y0}{Centre in npc [0,1] (scalars).}

\item{r, r0}{Outer and inner radius as a fraction of the smaller panel
dimension (\code{r0 = 0} gives a pie, \code{r0 > 0} a donut).}

\item{start, end}{Per-slice start/end angles in radians.}

\item{roughness, bowing, n_passes, seed}{Sketch parameters for the outline.}

\item{fill_style}{\code{"solid"} (default) paints each slice in its fill colour;
any other style (\code{"hachure"}, ...) hatches it instead.}

\item{hachure_angle, hachure_gap, fill_weight}{Fill parameters (\code{hachure_gap}
is a fraction of the smaller panel dimension).}

\item{fill_gp}{\code{gpar()} for the fill (per-slice \code{col} recycled).}

\item{outline_gp}{\code{gpar()} for the rough outline (per-slice \code{col} recycled).}

\item{name, vp}{Passed to \code{grid::gTree()}.}
}
\value{
A \code{SketchWedgeGrob} grob subclass.
}
\description{
Draws one or more annular sectors (pie or donut slices) sharing a centre,
guaranteed circular regardless of panel shape: radii are taken as a fraction
of the smaller panel dimension and the geometry is assembled in device
inches inside \code{makeContent()}, then roughened. Each slice's roughened
boundary is also reused as the fill region so the hand-drawn edge is kept.
}
\seealso{
Other grob-layer:
\code{\link[=sketch_arrow_grob]{sketch_arrow_grob()}},
\code{\link[=sketch_band_grob]{sketch_band_grob()}},
\code{\link[=sketch_callout_grob]{sketch_callout_grob()}},
\code{\link[=sketch_dotplot_grob]{sketch_dotplot_grob()}},
\code{\link[=sketch_ellipse_grob]{sketch_ellipse_grob()}},
\code{\link[=sketch_engrave_grob]{sketch_engrave_grob()}},
\code{\link[=sketch_path_grob]{sketch_path_grob()}},
\code{\link[=sketch_polygon_grob]{sketch_polygon_grob()}},
\code{\link[=sketch_spray_grob]{sketch_spray_grob()}},
\code{\link[=sketch_stroke_grob]{sketch_stroke_grob()}}
}
\concept{grob-layer}
