% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale-roughness.R
\name{scale_roughness_continuous}
\alias{scale_roughness_continuous}
\alias{scale_roughness}
\title{Continuous scale for the sketch \code{roughness} aesthetic}
\usage{
scale_roughness_continuous(..., range = c(0.01, 0.75), guide = "none")

scale_roughness(..., range = c(0.01, 0.75), guide = "none")
}
\arguments{
\item{...}{Other arguments passed to \code{\link[ggplot2:continuous_scale]{ggplot2::continuous_scale()}}.}

\item{range}{Output roughness range. Default \code{c(0.01, 0.75)}: \code{0.01} is
effectively clean and \code{0.75} is clearly hand-drawn without becoming noise.
Values above roughly \code{1} start to look scribbled.}

\item{guide}{Legend guide. Defaults to \code{"none"} because the legend keys do not
reflect roughness; set to \code{"legend"} to show one anyway.}
}
\value{
A ggplot2 scale object.
}
\description{
\code{\link[=geom_sketch_point]{geom_sketch_point()}} lets you map a variable to \code{roughness}
(\code{aes(roughness = z)}). This scale rescales that variable's observed range to a
legible band of roughness values, just as \code{scale_size()} rescales to a size
range. It is applied automatically whenever \code{roughness} is mapped to a
continuous variable, so you only call it directly to change \code{range}. To use
values as raw roughness with no rescaling, wrap them in \code{\link[base:I]{base::I()}}
(\code{aes(roughness = I(z))}).
}
\examples{
library(ggplot2)

# Mapped roughness is rescaled to c(0.01, 0.75) automatically.
ggplot(mtcars, aes(wt, mpg, roughness = hp)) +
  geom_sketch_point(size = 3, seed = 1L)

# Widen the band so the wobble difference is more dramatic.
ggplot(mtcars, aes(wt, mpg, roughness = hp)) +
  geom_sketch_point(size = 3, seed = 1L) +
  scale_roughness_continuous(range = c(0, 1.2))
}
\seealso{
Other sketch-theme:
\code{\link{CoordSketch}},
\code{\link{CoordSketchPolar}},
\code{\link[=element_sketch_line]{element_sketch_line()}},
\code{\link[=ggsketch_check_fonts]{ggsketch_check_fonts()}},
\code{\link[=register_sketch_font]{register_sketch_font()}},
\code{\link[=scale_pressure_continuous]{scale_pressure_continuous()}},
\code{\link{scale_sketch}},
\code{\link[=scale_tone_continuous]{scale_tone_continuous()}},
\code{\link[=sketch_palette]{sketch_palette()}},
\code{\link[=theme_sketch]{theme_sketch()}}
}
\concept{sketch-theme}
