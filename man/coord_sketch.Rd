% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coord-sketch.R
\docType{data}
\name{CoordSketch}
\alias{CoordSketch}
\alias{coord_sketch}
\title{A hand-drawn coordinate system}
\usage{
coord_sketch(
  xlim = NULL,
  ylim = NULL,
  expand = TRUE,
  default = FALSE,
  clip = "on",
  roughness = 0.5,
  bowing = 0.5,
  n_passes = 2L,
  seed = NULL,
  rough_grid = TRUE,
  rough_ticks = TRUE
)
}
\arguments{
\item{xlim, ylim}{Limits for the x and y axes (as in
\code{\link[ggplot2:coord_cartesian]{ggplot2::coord_cartesian()}}).}

\item{expand}{If \code{TRUE} (default), add the standard expansion around the data.}

\item{default}{Is this the default coordinate system? Default \code{FALSE}.}

\item{clip}{Should drawing be clipped to the panel (\code{"on"}, default) or not
(\code{"off"})?}

\item{roughness, bowing, n_passes}{Sketch parameters for the frame. Gentle
defaults suited to gridlines (\code{0.5}, \code{0.5}, \code{2}).}

\item{seed}{Integer seed for reproducible wobble. \code{NULL} uses
\code{getOption("ggsketch.seed", 1L)}.}

\item{rough_grid, rough_ticks}{Roughen the gridlines / axis ticks? Both default
\code{TRUE}; set one to \code{FALSE} to leave that element crisp.}
}
\value{
A \code{ggproto} Coord object to add to a plot.
}
\description{
A drop-in replacement for \code{\link[ggplot2:coord_cartesian]{ggplot2::coord_cartesian()}} that draws the \emph{frame}
hand-drawn: the panel gridlines and axis ticks are rendered as roughened
sketch grobs, so the frame matches the marks -- under any theme, not only
\code{\link[=theme_sketch]{theme_sketch()}}. It reuses ggplot2's own gridline and axis layout and only
swaps how those elements are drawn, so limits, expansion, and clipping behave
exactly like \code{coord_cartesian()}.
}
\details{
The panel \emph{border} is a plot-level theme element (not part of the coordinate
system), so to roughen it as well, combine \code{coord_sketch()} with
\code{theme_sketch(rough_frame = TRUE)} or set
\code{panel.border = element_sketch_rect(...)}.
}
\examples{
library(ggplot2)
# A rough frame under a plain (non-sketch) theme:
ggplot(mtcars, aes(wt, mpg)) +
  geom_sketch_point(seed = 1L) +
  coord_sketch(seed = 1L)
}
\seealso{
Other sketch-theme: 
\code{\link{element_sketch_line}()},
\code{\link{ggsketch_check_fonts}()},
\code{\link{register_sketch_font}()},
\code{\link{scale_roughness_continuous}()},
\code{\link{scale_sketch}},
\code{\link{scale_tone_continuous}()},
\code{\link{sketch_palette}()},
\code{\link{theme_sketch}()}
}
\concept{sketch-theme}
\keyword{datasets}
